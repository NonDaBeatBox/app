import { AppShell, TopBar } from '../components/AppShell'

// Placeholder — built in a later phase.
export default function Partner() {
  return (
    <AppShell>
      <TopBar title="Partner" back />
      <div className="p-6 text-muted">Partner coming up…</div>
    </AppShell>
  )
}
